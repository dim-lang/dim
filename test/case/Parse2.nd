def abs():void {
    var number:int = 3 + -2;
    number = 1 + 6e+4 / (-3) * (4 + 3 % -2) + 2;
    var a:int = number;
    var b:int = 10 * a + 10 * number / 4;
    var c:int = a - -b;
    ;
    ;
}

def World(x:int, y:int):int = {
    x = 1;
    y = 2;
    var z:uint = 2U;
    z = x + y;
    return z + 10 / 4;
}

/* import test.Parser1; */

def fail():void = {
    print("This is the very end of Parser2");
    print("Goodbye World!");
}
