def abs() {
    var number = 3 + -2;
    number = 1 + 6e+4 / (-3) * (4 + 3 % -2) + 2;
    var a = number;
    var b = 10 * a + 10 * number / 4;
    var c = a - -b;
    ;
    ;
}

def World(x, y) {
    x = 1;
    y = 2;
    var z = 2U;
    z = x + y;
    return z + 10 / 4;
}

/* import test.Parser1; */

def fail() {
    print("This is the very end of Parser2");
    print("Goodbye World!");
}
