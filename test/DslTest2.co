let abs() = {
    number = 3 + -2;
    number = 1 + 6e+4 / (-3) * (4 + 3 % -2) + 2;
    a = number;
    b = 10 * a + 10 * number / 4;
    c = a - -b;
    ;
    ;
}

let World(x, y) = {
    x = 1;
    y = 2;
    z = x + y;
    return z + 10 / 4;
}

/*import test.DslTest1;*/

let fail() = {
    print("This is the very end of DslTest2.fast");
    print("Goodbye World!");
}
